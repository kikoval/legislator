{% macro datetime(dt) %}
    {% if dt is null %}
        &ndash;
    {% else %}
        <time datetime="{{ dt|date('Y-m-d H:i') }}" title="{{ dt|date('d.m.Y H:i') }}">{{ dt|date('d.&\\nb\\sp;m.&\\nb\\sp;Y')|raw }}</time>
    {% endif %}
{% endmacro %}

{% extends "::base.html.twig" %}

{% block body %}
<div class="row">
    <div class="large-12 columns">
        <h1>Edit document</h1>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Save document" class="button"/>
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
